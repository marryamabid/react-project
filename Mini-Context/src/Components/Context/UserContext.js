import React from "react";

const UserContext= React.createContext()

export default UserContext

/**this gies us provider and we can wrap that inside userContextprovider 
 and inside we have component and we can access it from userContext  */ 